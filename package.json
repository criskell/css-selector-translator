{
  "private": true,
  "dependencies": {
    "css-what": "^6.1.0"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0",
    "jest": "^29.3.1",
    "parcel": "^2.8.2"
  },
  "source": "ui/index.html",
  "scripts": {
    "test": "jest",
    "ui:start": "parcel start",
    "ui:build": "parcel build",
    "deploy": "npm run ui:build && gh-pages -d ui"
  }
}
